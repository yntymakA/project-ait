DB_USER=marketplace_user
DB_PASSWORD=password
DB_NAME=marketplace
DATABASE_URL=mysql+pymysql://marketplace_user:password@db:3306/marketplace

APP_ENV=development
FIREBASE_PROJECT_ID=marketplace-ait
FIREBASE_STORAGE_BUCKET=marketplace-ait.firebasestorage.app
# Optional. Leave empty if there is no local Firebase Admin service account file.
FIREBASE_CREDENTIALS_PATH=
SECRET_KEY=supersecretkey-dev
# Comma-separated browser origins allowed to call the API (admin SPA, etc.)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
